@extends('layouts.app')
@section('title', 'Stock Counts')
@section('content')
<div class="container-fluid px-0"><div class="card shadow-sm border-0 mb-4"><div class="card-header bg-white"><h5 class="mb-0">Stock Count</h5></div><div class="card-body"><form method="POST" action="{{ route('admin.inventory.stock-counts.store') }}">@csrf
<table class="table table-sm"><thead><tr><th>Material</th><th>Lot</th><th>System</th><th>Counted</th><th>Reason</th></tr></thead><tbody>@foreach($balances as $balance)<tr><td>{{ $balance->internal_code }}</td><td>{{ $balance->lot_roll_no ?? '-' }}</td><td>{{ $balance->balance_qty }}</td><td><input type="hidden" name="items[{{ $balance->id }}][balance_id]" value="{{ $balance->id }}"><input type="number" step="0.0001" name="items[{{ $balance->id }}][counted_qty]" value="{{ $balance->balance_qty }}" class="form-control"></td><td><input name="items[{{ $balance->id }}][reason]" class="form-control"></td></tr>@endforeach</tbody></table><textarea name="notes" class="form-control mb-2" placeholder="Notes"></textarea><button class="btn btn-primary">Submit count</button></form></div></div>
<div class="card shadow-sm border-0"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Code</th><th>Status</th><th>Created</th><th></th></tr></thead><tbody>@foreach($counts as $count)<tr><td>{{ $count->count_code }}</td><td>{{ $count->status }}</td><td>{{ $count->created_at }}</td><td>@if($count->status==='submitted')<form method="POST" action="{{ route('admin.inventory.stock-counts.approve',$count->id) }}">@csrf<button class="btn btn-sm btn-success">Approve</button></form>@endif</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
